{
  "name": "pdf-document-parser",
  "version": "1.0.0",
  "private": true,
  "description": "Secure, profitable RapidAPI endpoint for parsing invoice and resume PDF structures into JSON.",
  "main": "server/index.js",
  "scripts": {
    "start": "node clean-frontend.js && node server/index.js",
    "dev": "nodemon server/index.js",
    "test": "node test/server.test.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "multer": "^2.0.0-rc.4",
    "pdf-parse": "^1.1.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "pdfkit": "^0.20.2"
  }
}
